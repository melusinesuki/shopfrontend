<view>
  <view>
    <view wx:for="{{products}}" wx:key="strNo" bind:tap="toDetail" data-no="{{item.strNo}}">
    <image src="{{item.strSurface}}"> </image>
    <view>{{item.strTitle}}</view>
    <view>{{item.intPrice / 100}}元</view>
    </view>
  </view>
</view>