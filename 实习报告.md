# 实习报告：正品酒仓微信小程序开发

## 一、实习概述

本次实习为期两天，主要任务是开发一个名为"正品酒仓"的微信小程序电商应用，包括后端 REST API 服务（Spring Boot + MyBatis + MySQL）和前端微信小程序（原生框架）两个部分。项目实现了商品列表展示和商品详情查看两个核心功能模块。

---

## 二、第一天实习内容：后端服务开发

### 1. 项目环境搭建

- 使用 Spring Initializr 创建 Spring Boot 4.0.6 项目，构建工具为 Maven
- 配置 Java 21 开发环境
- 在 `pom.xml` 中引入核心依赖：`spring-boot-starter-webmvc`（Web 框架）、`mybatis-spring-boot-starter` 4.0.1（ORM 持久层）、`mysql-connector-j`（MySQL 驱动）、`lombok`（简化代码）

### 2. 数据库配置

- 在 `application.yaml` 中配置数据源：连接本地 MySQL 数据库 `shop`，用户名为 `root`
- 数据库主机名配置为 `localhost`，端口为默认 3306
- Spring Boot 启动时自动完成数据库连接池初始化

### 3. 实体类设计

编写 `ProductBean.java` 商品实体类，使用 Lombok 注解 `@Data`、`@AllArgsConstructor`、`@NoArgsConstructor` 自动生成 getter/setter 和构造方法。包含五个字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| strNo | String | 商品编号 |
| strSurface | String | 商品图片 URL |
| strTitle | String | 商品名称 |
| intPrice | Integer | 商品价格（单位：分） |
| strUse | String | 商品介绍 |

### 4. REST API 接口开发

编写 `ProductController.java` 控制器类，使用 `@RestController` 注解，提供两个接口：

- **GET `/product/list`**：查询商品列表，返回包含 4 个商品（海盐薯片、橙汁饮料、新鲜苹果、洗衣液补充装）的 JSON 数组
- **POST `/product/detail`**：根据商品编号查询单个商品详情，使用 `@RequestBody` 接收 JSON 请求体，通过 `Objects.equals()` 遍历匹配并返回对应商品

接口使用 `@Slf4j` 注解记录日志，方便调试和追踪请求。

### 5. 接口测试

通过 `curl` 命令行工具对两个接口进行测试验证，确认返回数据格式正确、JSON 字段名与前端约定一致。

---

## 三、第二天实习内容：微信小程序前端开发

### 1. 项目初始化

- 在微信开发者工具中创建小程序项目，AppID 为 `wx18ab068e05871a22`
- 配置 `app.json`：注册两个页面路由、设置导航栏样式（白字深底）、启用 `glass-easel` 组件框架和 `v2` 样式
- 在 `app.js` 中实现小程序启动逻辑：初始化本地存储、调用 `wx.login()` 获取用户登录凭证

### 2. 商品列表页面开发

- **数据获取**：在 `onLoad` 生命周期中调用 `wx.request()` 向后端 `http://localhost:8080/product/list` 发起 GET 请求，将返回数据存入页面 `data.products`
- **模板渲染**：使用 `wx:for` 指令遍历商品数组，展示每个商品的图片 (`strSurface`)、名称 (`strTitle`)、价格（`intPrice / 100` 将分转为元）
- **页面跳转**：为每个商品项绑定 `bind:tap="toDetail"` 事件，通过 `data-no` 传递商品编号，调用 `wx.navigateTo()` 跳转到商品详情页

### 3. 商品详情页面开发

- **数据获取**：在 `onLoad` 中通过 `options.no` 接收列表页传递的商品编号，使用 POST 方式调用 `/product/detail` 接口获取商品详情
- **模板渲染**：展示商品大图、名称、价格（元为单位）、商品编号以及商品介绍（`strUse`）
- 使用 `wx:if` 条件渲染，仅在数据加载完成后显示页面

### 4. 前后端联调与问题修复

在联调过程中发现并解决了以下问题：

- 后端 `ProductController` 中 `detail()` 方法引用了未定义的变量 `list`，修复为在方法内部重新声明
- `Object.equals` 静态方法不存在，修正为 `java.util.Objects.equals()`
- `@RequestMapping` 默认只处理 GET 请求，修改为 `@PostMapping` 并添加 `@RequestBody` 注解以接收 JSON 请求
- 前端模板中字段名大小写不匹配（如 `item.strno` → `item.strNo`，`IntPrice` → `intPrice`）
- 详情页模板中使用了全角花括号 `｛｛｝｝`，修改为半角 `{{}}`
- 列表页跳转时参数名 `name` 与详情页读取的 `no` 不一致，统一为 `no`

---

## 四、实习体会与收获

### 技术层面

1. **掌握了 Spring Boot 快速开发 REST API 的完整流程**：从项目搭建、依赖管理、实体设计到 Controller 编写和接口测试，体验了 Spring Boot "约定优于配置" 的开发理念

2. **熟悉了微信小程序原生开发框架**：包括页面生命周期（`onLoad`、`onShow`）、数据绑定（`{{}}` 语法）、列表渲染（`wx:for`）、条件渲染（`wx:if`）、事件绑定（`bind:tap`）和页面路由（`wx.navigateTo`）等核心机制

3. **理解了前后端分离架构的协作方式**：前端通过 HTTP 请求调用后端 REST API，JSON 作为数据交换格式，前后端通过约定的字段名进行通信

4. **学习了 Lombok、MyBatis 等常用框架的基本使用**：Lombok 大幅减少了样板代码，MyBatis 提供了灵活的数据库访问能力

### 工程实践层面

5. **增强了调试与排错能力**：在前后端联调过程中，遇到了变量作用域、方法引用错误、字段名大小写不匹配、字符编码等多类型问题，通过查看编译日志和运行时日志逐一定位并解决

6. **认识到前后端接口规范的重要性**：请求方法（GET/POST）、参数传递方式（Query String vs JSON Body）、字段命名风格（驼峰命名）等细节需要在开发前明确约定，才能避免联调时的不必要返工

---

## 五、感受与反思

通过两天的实习，我对企业级应用开发的完整流程有了更直观的认识。从后端的数据库设计、接口开发到前端页面的布局与交互，每一个环节都需要严谨细致。最大的感触是：**代码编写只是开发工作的一部分，调试和问题排查往往占据更多时间**。

在遇到编译错误和联调问题时，起初有些手足无措，但通过仔细阅读错误日志、逐行检查代码逻辑，最终都找到了问题根源。这让我意识到，**良好的日志习惯和对框架原理的理解是高效排错的关键**。

此外，这次实习也让我看到了理论与实践之间的差距。课堂上学到的 Java 和数据库知识是基础，但在实际项目中，需要综合运用 Maven、Spring Boot、MyBatis、微信开发者工具等多种工具和框架，对开发者的工程能力和学习能力提出了更高要求。

---

## 六、项目技术栈总结

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 4.0.6 | 项目基础框架 |
| 构建工具 | Maven (mvnw) | 依赖管理与构建 |
| 持久层 | MyBatis 4.0.1 | ORM 框架（已配置，待接入） |
| 数据库 | MySQL 9.6 | 关系型数据库 |
| 开发语言 | Java 21 | 后端开发语言 |
| 简化代码 | Lombok | 自动生成 getter/setter/构造方法 |
| 前端框架 | 原生微信小程序 (glass-easel) | 小程序组件框架 |
| 前端语言 | JavaScript + WXML + WXSS | 小程序前端三件套 |
| 开发工具 | 微信开发者工具 + IntelliJ IDEA | IDE |
